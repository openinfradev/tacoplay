# Download URLs
# DO NOT TOUCH BELOWS!!
# Below lines are auto generated by jenkins
# Refer to kubespray/roles/download/defaults/main.yml for the original URLs
image_arch: "{{host_architecture | default('amd64')}}"
kubeadm_download_url: "http://{{k8s_binary_repo_url}}/k8s/kubeadm-{{kubeadm_version}}-{{image_arch}}"
hyperkube_download_url: "http://{{k8s_binary_repo_url}}/k8s/hyperkube-{{kube_version}}-{{image_arch}}"
etcd_download_url: "http://{{k8s_binary_repo_url}}/k8s/etcd-{{etcd_version}}-linux-amd64.tar.gz"
cni_download_url: "http://{{k8s_binary_repo_url}}/k8s/cni-plugins-linux-{{image_arch}}-{{cni_version}}.tgz"
calicoctl_download_url: "http://{{k8s_binary_repo_url}}/k8s/calicoctl"
crictl_download_url: "http://{{k8s_binary_repo_url}}/k8s/crictl-{{crictl_version}}-linux-{{image_arch}}.tar.gz"
