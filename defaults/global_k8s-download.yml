# Download URLs
# DO NOT TOUCH BELOWS!!
# Below lines are auto generated by jenkins
# Refer to kubespray/roles/download/defaults/main.yml for the original URLs
image_arch: "{{host_architecture | default('amd64')}}"
kubeadm_download_url: >-
  {%- if local_k8s_binary_repo_enabled -%}http://{{k8s_binary_repo_url}}/k8s/kubeadm-{{kubeadm_version}}-{{image_arch}}
  {%- else -%}https://storage.googleapis.com/kubernetes-release/release/{{kubeadm_version}}/bin/linux/{{image_arch}}/kubeadm{%- endif -%}
hyperkube_download_url: >-
  {%- if local_k8s_binary_repo_enabled -%}http://{{k8s_binary_repo_url}}/k8s/hyperkube-{{kube_version}}-{{image_arch}}
  {%- else -%}https://storage.googleapis.com/kubernetes-release/release/{{kube_version}}/bin/linux/{{image_arch}}/hyperkube{%- endif -%}
etcd_download_url: >-
  {%- if local_k8s_binary_repo_enabled -%}http://{{k8s_binary_repo_url}}/k8s/etcd-{{etcd_version}}-linux-amd64.tar.gz
  {%- else -%}https://github.com/coreos/etcd/releases/download/{{etcd_version}}/etcd-{{etcd_version}}-linux-{{image_arch}}.tar.gz{%- endif -%}
cni_download_url: >-
  {%- if local_k8s_binary_repo_enabled -%}http://{{k8s_binary_repo_url}}/k8s/cni-plugins-linux-{{image_arch}}-{{cni_version}}.tgz
  {%- else -%}https://github.com/containernetworking/plugins/releases/download/{{cni_version}}/cni-plugins-linux-{{image_arch}}-{{cni_version}}.tgz{%- endif -%}
calicoctl_download_url: >-
  {%- if local_k8s_binary_repo_enabled -%}http://{{k8s_binary_repo_url}}/k8s/calicoctl
  {%- else -%}https://github.com/projectcalico/calicoctl/releases/download/{{calico_ctl_version}}/calicoctl-linux-{{image_arch}}{%- endif -%}
crictl_download_url: >-
  {%- if local_k8s_binary_repo_enabled -%}http://{{k8s_binary_repo_url}}/k8s/crictl-{{crictl_version}}-linux-{{image_arch}}.tar.gz
  {%- else -%}https://github.com/kubernetes-sigs/cri-tools/releases/download/{{crictl_version}}/crictl-{{crictl_version}}-{{ansible_system|lower}}-{{image_arch}}.tar.gz{%- endif -%}
