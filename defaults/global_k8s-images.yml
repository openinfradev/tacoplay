armada_image_repo: "{{ container_registries[0].endpoint }}/sktdev/armada"
openstackclient_image_repo: "{{ container_registries[0].endpoint }}/sktdev/openstackclient"
rbd_provisioner_image_repo: "{{ container_registries[0].endpoint }}/sktdev/rbd-provisioner"

# Containers
# DO NOT TOUCH BELOWS!!
# Below lines are auto generated by jenkins
# Refer to kubespray/roles/download/defaults/main.yml for the original image URLs
kube_image_repo: "{{ container_registries[0].endpoint }}/google-containers"
etcd_image_repo: "{{ container_registries[0].endpoint }}/coreos/etcd"
flannel_image_repo: "{{ container_registries[0].endpoint }}/coreos/flannel"
flannel_cni_image_repo: "{{ container_registries[0].endpoint }}/coreos/flannel-cni"
calico_node_image_repo: "{{ container_registries[0].endpoint }}/calico/node"
calico_cni_image_repo: "{{ container_registries[0].endpoint }}/calico/cni"
calico_policy_image_repo: "{{ container_registries[0].endpoint }}/calico/kube-controllers"
calico_rr_image_repo: "{{ container_registries[0].endpoint }}/calico/routereflector"
calico_typha_image_repo: "{{ container_registries[0].endpoint }}/calico/typha"
pod_infra_image_repo: "{{ container_registries[0].endpoint }}/google_containers/pause-amd64"
install_socat_image_repo: "{{ container_registries[0].endpoint }}/xueshanf/install-socat"
netcheck_agent_image_repo: "{{ container_registries[0].endpoint }}/l23network/k8s-netchecker-agent"
netcheck_server_image_repo: "{{ container_registries[0].endpoint }}/l23network/k8s-netchecker-server"
weave_kube_image_repo: "{{ container_registries[0].endpoint }}/weaveworks/weave-kube"
weave_npc_image_repo: "{{ container_registries[0].endpoint }}/weaveworks/weave-npc"
contiv_image_repo: "{{ container_registries[0].endpoint }}/contiv/netplugin"
contiv_init_image_repo: "{{ container_registries[0].endpoint }}/contiv/netplugin-init"
contiv_auth_proxy_image_repo: "{{ container_registries[0].endpoint }}/contiv/auth_proxy"
contiv_etcd_init_image_repo: "{{ container_registries[0].endpoint }}/ferest/etcd-initer"
contiv_ovs_image_repo: "{{ container_registries[0].endpoint }}/contiv/ovs"
cilium_image_repo: "{{ container_registries[0].endpoint }}/cilium/cilium"
cilium_init_image_repo: "{{ container_registries[0].endpoint }}/cilium/cilium-init"
cilium_operator_image_repo: "{{ container_registries[0].endpoint }}/cilium/operator"
kube_ovn_db_image_repo: "{{ container_registries[0].endpoint }}/kubeovn/kube-ovn-db"
kube_ovn_node_image_repo: "{{ container_registries[0].endpoint }}/kubeovn/kube-ovn-node"
kube_ovn_cni_image_repo: "{{ container_registries[0].endpoint }}/kubeovn/kube-ovn-cni"
kube_ovn_controller_image_repo: "{{ container_registries[0].endpoint }}/kube-ovn-controller"
kube_router_image_repo: "{{ container_registries[0].endpoint }}/cloudnativelabs/kube-router"
multus_image_repo: "{{ container_registries[0].endpoint }}/nfvpe/multus"
nginx_image_repo: "{{ container_registries[0].endpoint }}/nginx"
haproxy_image_repo: "{{ container_registries[0].endpoint }}/haproxy"
coredns_image_repo: "{{ container_registries[0].endpoint }}/coredns/coredns"
nodelocaldns_image_repo: "{{ container_registries[0].endpoint }}/k8s-dns-node-cache"
dnsautoscaler_image_repo: "{{ container_registries[0].endpoint }}/cluster-proportional-autoscaler-amd64"
test_image_repo: "{{ container_registries[0].endpoint }}/busybox"
busybox_image_repo: "{{ container_registries[0].endpoint }}/busybox"
helm_image_repo: "{{ container_registries[0].endpoint }}/lachlanevenson/k8s-helm"
tiller_image_repo: "{{ container_registries[0].endpoint }}/kubernetes-helm/tiller"
registry_image_repo: "{{ container_registries[0].endpoint }}/registry"
registry_proxy_image_repo: "{{ container_registries[0].endpoint }}/google_containers/kube-registry-proxy"
metrics_server_image_repo: "{{ container_registries[0].endpoint }}/google_containers/metrics-server-amd64"
local_volume_provisioner_image_repo: "{{ container_registries[0].endpoint }}/external_storage/local-volume-provisioner"
cephfs_provisioner_image_repo: "{{ container_registries[0].endpoint }}/external_storage/cephfs-provisioner"
rbd_provisioner_image_repo: "{{ container_registries[0].endpoint }}/external_storage/rbd-provisioner"
local_path_provisioner_image_repo: "{{ container_registries[0].endpoint }}/rancher/local-path-provisioner"
ingress_nginx_controller_image_repo: "{{ container_registries[0].endpoint }}/kubernetes-ingress-controller/nginx-ingress-controller"
cert_manager_controller_image_repo: "{{ container_registries[0].endpoint }}/jetstack/cert-manager-controller"
addon_resizer_image_repo: "{{ container_registries[0].endpoint }}/addon-resizer"
dashboard_image_repo: "{{ container_registries[0].endpoint }}/google_containers/kubernetes-dashboard-amd64"
