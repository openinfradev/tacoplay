apiVersion: storage.k8s.io/v1beta1
kind: StorageClass
metadata:
  name: "{{ storageclass_name }}"
  annotations:
    storageclass.beta.kubernetes.io/is-default-class: "true"
provisioner: kubernetes.io/rbd
parameters:
  monitors: "{{ monitors }}"
  adminId: "{{ admin_id }}"
  adminSecretName: "{{ admin_secret_name }}"
  adminSecretNamespace: "{{ admin_secret_namespace }}"
  pool: "{{ pool_name }}"
  userId: "{{ user_id }}"
  userSecretName: "{{ user_secret_name }}"
  imageFormat: "{{ image_format }}"
  imageFeatures: "{{ image_feature }}"

