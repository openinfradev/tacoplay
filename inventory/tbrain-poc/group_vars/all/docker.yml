# docker options for kubespray and registry

# override docker repo to localrepo when outbound access is not allowed
#docker_rh_repo_base_url: http://localrepo/docker/linux/centos/7/$basearch/stable
#docker_rh_repo_gpgkey: http://localrepo/docker/linux/centos/gpg
#dockerproject_rh_repo_base_url: http://localrepo/dockerproject/repo/main/centos/7
#dockerproject_rh_repo_gpgkey: http://localrepo/dockerproject/gpg

## Enable docker_container_storage_setup, it will configure devicemapper driver on Centos7 or RedHat7.
docker_container_storage_setup: false
#docker_container_storage_setup_devs: /dev/vdb

docker_version: "18.06"
docker_daemon_graph: "/var/lib/docker"
docker_log_opts: "--log-opt max-size=50m --log-opt max-file=5"
docker_insecure_registries:
  - tacorepo:5000

## A string of extra options to pass to the docker daemon.
## This string should be exactly as you wish it to appear.
docker_options: >-
  {%- if docker_insecure_registries is defined -%}
  {{ docker_insecure_registries | map('regex_replace', '^(.*)$', '--insecure-registry=\1' ) | list | join(' ') }}
  {%- endif %}
  {% if docker_registry_mirrors is defined -%}
  {{ docker_registry_mirrors | map('regex_replace', '^(.*)$', '--registry-mirror=\1' ) | list | join(' ') }}
  {%- endif %}
  --graph={{ docker_daemon_graph }} {{ docker_log_opts }}
  --default-ulimit nofile=1024:1048576
  {%- if ansible_architecture == "aarch64" and ansible_os_family == "RedHat" %}
  --add-runtime docker-runc=/usr/libexec/docker/docker-runc-current
  --default-runtime=docker-runc --exec-opt native.cgroupdriver=systemd
  --userland-proxy-path=/usr/libexec/docker/docker-proxy-current --signature-verification=false
  {%- endif -%}
